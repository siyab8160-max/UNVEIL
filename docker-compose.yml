services:
  postgres:
    image: postgres:16-alpine
    container_name: certledger-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: certledger
    ports:
      - "5432:5432"
    volumes:
      - certledger_pgdata:/var/lib/postgresql/data

volumes:
  certledger_pgdata:
